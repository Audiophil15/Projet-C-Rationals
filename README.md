# Minilib to use rational numbers in C++ for IMAC 2nd year first prog project

Folder is structured like this :

```sh
Project
    `--include
        `--rationals
    `--src
        `--unitests
    `--test
```

Compilation is done with :
```sh
mkdir build && cd build
cmake .. && cmake --build .
```


Author : Philippe Rosales